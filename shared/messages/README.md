# Agent Mailbox

Use one file per message instead of both agents editing the same mailbox file.

Suggested path:
`shared/messages/<sender>/<YYYYMMDD-HHMM>-<topic>.md`

This reduces write conflicts and keeps a permanent collaboration history.
